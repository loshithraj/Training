# 8 Cell Colony Problem

cells = list(map(int, input("Enter 8 cell states (0 or 1): ").split()))
days = int(input("Enter number of days: "))

for _ in range(days):
    new_cells = [0] * 8
    for i in range(1, 7):
        if cells[i-1] == cells[i+1]:
            new_cells[i] = 0
        else:
            new_cells[i] = 1
    cells = new_cells

print("Final cell state:", cells)
