# day-3

1.Given two strings X and Y, write a program to find:
   *The length of their Longest Common Subsequence (LCS).
    *One valid LCS string.

Input:
 *Two strings X and Y entered by the user.
 *Strings can contain uppercase or lowercase letters.

Output:
 *Print the length of the LCS.
 *Print one LCS string.
